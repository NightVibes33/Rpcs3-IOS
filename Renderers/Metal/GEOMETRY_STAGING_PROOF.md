# Geometry staging proof

This branch exists only to trigger a pull-request workflow against the frozen `metal-geometry-staging` base. It validates RPCS3 draw-array, indexed-array, inline-array, vertex-layout, and staged-buffer compilation without merging this marker into the renderer branch.
